[project]
name = "study-app"
version = "0.1.0"
description = "Children's learning app with flashcards and dictionary"
requires-python = ">=3.10,<3.13"
dependencies = [
    "kivy>=2.3.0,<3.0",
    "plyer>=3.1.0",
    "requests>=2.28",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
